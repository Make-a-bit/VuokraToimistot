// The leftButton variable is an object representing CSS style properties for a left-aligned button.
/**
 * @type {{marginBottom: string, marginLeft: string}}
 */
export const leftButton = {
    marginBottom: "10px",
    marginLeft: "15px"
}

// The middleButton variable is an object representing CSS style properties for a middle-aligned button.
/**
 * @type {{marginBottom: string}}
 */
export const middleButton = {
    marginBottom: "10px"
}

// The rightButton variable is an object representing CSS style properties for a right-aligned button.
/**
 * @type {{marginBottom: string, marginRight: string}}
 */
export const rightButton = {
    marginBottom: "10px",
    marginRight: "15px"
}

// The autoCompleteFieldMargins variable is an object representing CSS style properties for an autocomplete field.
/**
 * @type {{marginBottom: string, marginRight: string, minWidth: string}}
 */
export const autoCompleteFieldMargins = {
    marginBottom: "10px",
    marginRight: "10px",
    minWidth: "350px",
}